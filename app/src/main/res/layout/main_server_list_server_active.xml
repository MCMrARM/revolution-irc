<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="server" type="io.mrarm.irc.ServerConnectionInfo"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="?attr/colorBackgroundFloating">

        <io.mrarm.irc.view.ServerIconView
            android:id="@+id/icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:layout_margin="12dp"
            android:textSize="20sp"
            app:serverName="@{server.getName()}"
            tools:serverName="M" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_gravity="center">

            <TextView
                android:id="@+id/name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.AppCompat.Body1"
                android:text="@{server.getName()}"
                tools:text="Server name" />

            <TextView
                android:id="@+id/desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{@string/server_list_channel_counter_with_state(@plurals/server_list_channel_counter(server.getChannelCount(), server.getChannelCount()), context.getString(server.getStatusResId()))}"
                tools:text="Description" />

        </LinearLayout>

    </LinearLayout>

</layout>